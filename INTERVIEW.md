# 面试演示话术(供参考)

> 每个产品页建议 2-3 分钟,4 个产品 + 主页架构总览 = 12-15 分钟讲解

## 开场(主页 1 分钟)

"这是一个围绕航空维修企业真实业务场景做的 4 个 AI 工程作品。
技术上前后端分离,前端 Next.js 14 + React Flow 架构图 + 流式对话,
后端 FastAPI + 智谱 GLM-4 + Chroma 向量库 + XGBoost。
4 个产品全部全栈可交互,演示时会调真实的 LLM 和真实模型推理。"

## 1. AI 智库机器人(2 分钟)

**痛点开场**:
"机务维修人员每天要查 AMM、维修方案、工卡,跨 6+ 个系统,
人工查找平均 15 分钟,信息分散且版本不一致。
我主导设计了一个 RAG + 意图分类的本地化方案。"

**架构图讲解**:
- "左侧意图识别把问题分成 5 类(工时/排故/手册/工卡/通用),走不同检索路径"
- "中间是双源检索:向量召回 + 关键词精排,Top-3 重排序后给 LLM"
- "Prompt 强制要求引用溯源 + 免责声明,降低幻觉"
- "右边 GLM-4-Flash 流式输出,2 秒内首 token"

**演示**:
点击"波音 737NG 起落架更换工时"→ 看到意图分类、检索得分、流式回答、引用源标注。

**亮点**:
- 手册数据本地化(向量库),敏感数据不出网
- 引用溯源让机务可验证
- 持续监控检索命中率,反向优化分块

## 2. AI 营销内容生成器(2 分钟)

**痛点**:
"营销每月出 300+ 素材,跨 4 个平台,品牌一致性难保证,法务驳回率高。"

**方案**:
"MVP 阶段直接调 API(月成本 300 元 vs 自建 GPU 3000+),
把品牌规则抽象成 JSON(禁用词、标志词、语气),运营自己改。
多模态模板(小红书/公众号/抖音)按平台调性结构化 Prompt。"

**演示**:
输入"618 母婴洗衣液种草" → 选小红书 + 默认品牌 → 流式生成
→ 展示禁用词/标志词约束 → 采纳/驳回按钮模拟数据回写

**亮点**:
- 成本测算驱动选型(MVP API / 长期私有化)
- 风格规则即代码,运营可自助维护
- 采纳率作为北极星指标,反向迭代

## 3. AI 生产分析助手(2 分钟)

**痛点**:
"管理者 80% 临时数据需求走人工提单,响应 4-8 小时,排产决策滞后。"

**方案**:
"NL2SQL 方案:协同数据团队梳理 ERP/MES/QMS 3 套系统的 Schema,
Few-shot + 强制只读 + LIMIT 50 保护,SQL 生成后做 AST 校验拒绝危险语句。"

**演示**:
问"上月各产线 OEE 排名" → 展示自动生成的 SQL + 执行结果表格
→ 切到查询日志 → 看到高频查询已自动建议固化为报表

**亮点**:
- 安全性:白名单 + 关键字过滤 + LIMIT
- 持续优化:日志聚类推动报表固化,降低调用量
- 透明:SQL 回显让业务验证逻辑

## 4. AI 供应链需求预测(2 分钟)

**痛点**:
"航材备库成本 vs 缺料风险难以平衡,过度备库年成本超千万。"

**方案**:
"XGBoost 构建时序模型,特征融合业务假设(定检计划/季节性/机队规模),
分场景评价:安全件(0 漏报)vs 消耗件(控误报率)。
硬件复用现有 CPU 服务器,单次推理 < 50ms,无 GPU 成本。"

**演示**:
切换安全件/消耗件 → 调整定检级别、季节系数
→ 看到预测值 + P85 区间 + 特征重要性条形图

**亮点**:
- 业务特征 > 纯时序,MAPE 优于 ARIMA 30%+
- 分场景评价,贴合实际业务诉求
- 特征重要性可解释,业务可参与调优

## 收尾(1 分钟)

"4 个产品共用一套工程基座:
- 统一 LLM 客户端(LLMClient)→ 后续切模型只改 env
- 统一 SSE 流式输出 → 前端体验一致
- 统一配置/启动脚本 → 5 分钟跑起来
- 真实可交互的演示 → 面试官可亲自点"

"如果进团队,我能在 2 周内基于这套基建产出 POC,
3 个月可达到生产化部署。"

## 常见追问 Q&A

**Q1: 为什么 RAG 而不是微调?**
- 微调成本高、迭代慢,机务手册每月更新,RAG 即时索引即可
- 黑盒模型难做引用溯源,机务需要看到依据
- 数据安全:手册不出本地

**Q2: GLM-4-Flash 是否够用?为什么不上 GPT-4?**
- Flash 速度更快(2s 首 token),面试演示体感好
- 任务复杂度可控(RAG/NL2SQL 都是结构化场景)
- 数据合规:航司对数据出境敏感,国产模型更安全
- 业务代码解耦,通过 LLMClient 切换,后续可平滑升级到 Plus

**Q3: NL2SQL 的准确率怎么保证?**
- Few-shot 给标准模式
- AST 校验拦截危险 SQL
- SQL 回显让人参与审核
- 失败 case 沉淀到日志,反向优化 prompt 和 schema 描述

**Q4: XGBoost vs 深度学习时序模型?**
- 数据量 2000 行,DL 容易过拟合
- XGBoost 可解释,业务能参与调特征
- 部署简单,CPU 推理 < 50ms
- 后续数据量起来(>10万)再考虑 Transformer

**Q5: 4 个产品如何协同?**
- 共享 LLM 客户端 → 统一升级
- 共享 SSE 流式协议 → 前后端复用
- 共享数据/模型管理脚本 → 工程化
- 共享前端组件库(对话/图表/架构图)
